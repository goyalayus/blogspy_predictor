{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "gridPos": { "h": 2, "w": 24, "x": 0, "y": 0 },
      "id": 1,
      "options": { "content": "# Python ML API: Deep Dive", "mode": "markdown" },
      "type": "text"
    },
    {
      "title": "API Request Rate (RED)",
      "type": "timeseries",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 2 },
      "id": 2,
      "options": {
        "legend": { "displayMode": "table", "placement": "bottom" },
        "tooltip": { "mode": "multi" }
      },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "Prometheus" },
          "expr": "sum(rate(http_requests_total{job=\"python-api\"}[5m]))",
          "legendFormat": "Total Requests / sec"
        },
        {
          "datasource": { "type": "prometheus", "uid": "Prometheus" },
          "expr": "sum(rate(http_requests_total{job=\"python-api\", status_code=~\"5..\"}[5m]))",
          "legendFormat": "5xx Errors / sec"
        }
      ]
    },
    {
      "title": "API Request Duration (p99)",
      "type": "timeseries",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 2 },
      "id": 3,
      "options": {
        "legend": { "displayMode": "list", "placement": "bottom" },
        "tooltip": { "mode": "multi" }
      },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "Prometheus" },
          "expr": "histogram_quantile(0.99, sum by (le) (rate(http_requests_latency_seconds_bucket{job=\"python-api\"}[5m])))",
          "legendFormat": "p99 Latency"
        }
      ],
      "fieldConfig": { "defaults": { "unit": "s" } }
    },
    {
      "title": "ML Pipeline Duration (p95)",
      "type": "timeseries",
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "gridPos": { "h": 10, "w": 12, "x": 0, "y": 10 },
      "id": 4,
      "options": {
        "legend": { "displayMode": "list", "placement": "bottom" },
        "tooltip": { "mode": "multi" }
      },
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "Prometheus" },
          "expr": "histogram_quantile(0.95, sum by (le) (rate(blogspy_ml_pipeline_duration_seconds_bucket[5m])))",
          "legendFormat": "p95 ML Pipeline Duration"
        }
      ],
      "fieldConfig": { "defaults": { "unit": "s" } }
    },
    {
  "title": "Feature Engineering Breakdown (p95 Duration)",
  "type": "timeseries",
  "datasource": { "type": "prometheus", "uid": "Prometheus" },
  "gridPos": { "h": 10, "w": 12, "x": 12, "y": 10 },
  "id": 5,
  "options": {
    "legend": { "displayMode": "table", "placement": "bottom" },
    "tooltip": { "mode": "multi" },
     "stacking": { "mode": "normal" }
  },
  "targets": [
    {
      "datasource": { "type": "prometheus", "uid": "Prometheus" },
      "expr": "histogram_quantile(0.95, sum by (le, step) (rate(blogspy_ml_feature_engineering_duration_seconds_bucket[5m])))",
      "legendFormat": "{{step}}"
    }
  ],
  "fieldConfig": { "defaults": { "unit": "s" } }
}
  ],
  "refresh": "1m",
  "schemaVersion": 39,
  "tags": [ "blogspy", "python", "ml", "deep-dive" ],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "BlogSpy - Python ML API Deep Dive",
  "uid": "blogspy-python-deep-dive",
  "version": 1
}
